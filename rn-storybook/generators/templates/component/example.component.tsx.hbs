import * as S from './{{kebabCase name}}.styles'

export interface {{pascalCase name}}Props {
  testId?: string
}

export const {{pascalCase name}} = ({ testId = 'component-{{kebabCase name}}-id', ...props }: {{pascalCase name}}Props) => {
  return (
    <>
      <S.Wrapper data-testid={testId} {...props}>
        Body...
      </S.Wrapper>
    </>
  )
}
